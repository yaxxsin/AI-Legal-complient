# ============================================
# LocalCompliance — Docker Compose Override
# Server Deployment Configuration
# ============================================

services:
  web:
    build:
      args:
        # API URL untuk browser (client-side)
        NEXT_PUBLIC_API_URL: https://api.locallitix.my.id/api/v1
